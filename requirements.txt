# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Trascrizione (open-source, nessun token)
# faster-whisper: ~4x più veloce di openai-whisper su CPU grazie a CTranslate2 + int8
faster-whisper>=1.0.0

# Diarizzazione speaker (nessun token)
resemblyzer>=0.1.4

# ML / numerics
scikit-learn>=1.4.0
numpy>=1.26.0,<2.0
scipy>=1.12.0

# Audio / video (usa ffmpeg come backend)
torch>=2.1.0           # richiesto da resemblyzer
torchaudio>=2.1.0
