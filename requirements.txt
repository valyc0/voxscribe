# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Trascrizione (open-source, nessun token)
# I modelli vengono scaricati automaticamente da GitHub Releases alla prima esecuzione
openai-whisper>=20231117

# Diarizzazione speaker (nessun token)
resemblyzer>=0.1.4

# ML / numerics
scikit-learn>=1.4.0
numpy>=1.26.0,<2.0
scipy>=1.12.0

# Audio / video (usa ffmpeg come backend)
torch>=2.1.0           # richiesto da Whisper e resemblyzer
torchaudio>=2.1.0
